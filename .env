# === 必须填写 ===
# PRIVATE_KEY：强烈建议使用「全新、小额的 MetaMask 钱包」私钥
# 千万不要使用你的主钱包！一旦私钥外泄，钱包内所有资产都会被盗
# 操作步骤：MetaMask → 新建账户 → 转入少量 USDC → 导出私钥（Export Private Key）
PRIVATE_KEY=0x你的全新小额burner钱包私钥（64位十六进制，不要有空格）

# 要跟单的目标钱包地址（多个用逗号分隔，建议1~5个）
TARGET_WALLETS=0x9d3e989dd42030664e6157dae42f6d549542c49e

# === 可选参数（强烈建议调整，控制风险） ===
TRADE_MULTIPLIER=0.35           # 跟单比例（0.1~0.5 建议，小额测试先用0.2）
MIN_TRADE_USD=10                # 单笔最小跟单金额（太低容易跟垃圾单，太高漏机会）
MAX_TRADE_USD=150               # 单笔最大跟单金额（防止大单爆仓）
MAX_POSITION_USD=500            # 总持仓上限（所有市场总金额，防止过度暴露）
SLIPPAGE_TOLERANCE=0.02         # 滑点容忍度（2% 比较合理）

PAPER_MODE=true                 # true=模拟（推荐先跑很久测试），false=实盘

# === RPC wss 端点（可选，手动配置优先） ===
# 如果留空，脚本用默认公共端点
RPC_WSS_LIST=

# === 脚本自动生成/手动填写，不要乱改 ===
# 如果自动生成失败，手动去网站生成后填入这里
API_KEY=
API_SECRET=
API_PASSPHRASE=
