# === 必须填写 ===
# PRIVATE_KEY：强烈建议使用「全新、小额的 MetaMask 钱包」私钥
# 千万不要使用你的主钱包！一旦私钥外泄，钱包内所有资产都会被盗
# 操作步骤：MetaMask → 新建账户 → 转入少量 USDC → 导出私钥（Export Private Key）
PRIVATE_KEY=0x你的全新小额burner钱包私钥（64位十六进制，不要有空格）

# 要跟单的目标钱包地址（多个用逗号分隔，建议1~5个）
# 示例：你的目标地址
TARGET_WALLETS=0x9d3e989dd42030664e6157dae42f6d549542c49e

# === 多 RPC wss 端点列表（用逗号分隔，按顺序尝试，第一个优先） ===
# 用户手动配置优先（这里填写你想用的地址，可以随时修改）
# 如果留空或删除这一行，脚本会使用代码中的默认公共端点
# 推荐格式：wss://地址1,wss://地址2,wss://地址3（无空格）
RPC_WSS_LIST=wss://polygon-mainnet.g.alchemy.com/v2/-bEz16JgshTl-2CIg405T,wss://ws-mainnet.matic.network,wss://polygon-mainnet.public.blastapi.io,wss://ws-polygon-mainnet.bwarelabs.com

# === 可选参数（建议调整，控制风险） ===
# 跟单比例（例如 0.35 = 对方下100刀，你下35刀，建议0.1~0.5）
TRADE_MULTIPLIER=0.35

# 单笔最大跟单金额（美元，强烈建议50~200，防止爆仓）
MAX_POSITION_USD=150

# 目标交易金额小于此值不跟（过滤小单，建议10~50）
MIN_TRADE_USD=20

# true = 模拟模式（只打印不下真单），false = 真实交易
# 先保持 true 测试很久，确认一切正常再改 false
PAPER_MODE=true

# 滑点容忍度（例如 0.02 = 2%）
SLIPPAGE_TOLERANCE=0.02

# === 脚本自动生成，不要手动填写 ===
# 第一次运行选项3时会自动填入值
API_KEY=
API_SECRET=
API_PASSPHRASE=
